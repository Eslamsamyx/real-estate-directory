# Engine clone — we don't track upstream source
engine/

# Brainstorm session artifacts
.superpowers/

# Playwright MCP screenshots saved at root
*.png
!realestate/**/*.png
!realestate/*.png
!demo/thumbs/*.png

# Playwright MCP runtime artifacts (sessions, traces)
.playwright-mcp/

# OS noise
.DS_Store
